import { combineReducers } from 'redux';

const dummyReducer = (state) => state;

// uncomment when needed
// const reducers = combineReducers({});
const reducers = dummyReducer;

export default reducers;
